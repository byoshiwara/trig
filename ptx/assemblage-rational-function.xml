<?xml version="1.0" encoding="UTF-8" ?>

<assemblage>
    <title>Rational Function</title>
    <p>
		A <term>rational function</term><idx>rational function</idx> is one of the form
		<me>
			f (x) =\frac{P(x)}{Q(x)}
		</me>
		where <m>P(x)</m> and <m>Q(x)</m> are polynomials and <m>Q(x)</m> is not the zero polynomial.
    </p>
</assemblage>
